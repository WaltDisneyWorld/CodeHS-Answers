function start(){
    var enoughCredits = readBoolean("Do you have enough Credits? ");
	var reqs = readBoolean("Do you meet the requirements? ");
	var graduation = (enoughCredits && reqs);
	if(!enoughCredits && reqs){
	    var graduation = false;
	}
	print ("Can you Graduate? " + graduation);

}
